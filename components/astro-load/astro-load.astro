---
interface Props {
	data: any;
	id: string;
}

const { id, data } = Astro.props;
---

<script define:vars={{ id, data }}>
	const load = window.__AstroLoad__ || (window.__AstroLoad__ = {});
	const info = load[id] || (load[id] = {});

	info.data = data;

	if (info.load) info.load(id);
</script>
